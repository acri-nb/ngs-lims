# Generated by Django 4.2.30 on 2026-07-24 12:07

from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ('library', '0009_alter_workflowtypestep_is_stopping_point'),
    ]

    operations = [
        migrations.AddField(
            model_name='libraryprepsample',
            name='insufficientMaterial',
            field=models.BooleanField(default=False, help_text="True when the sample's total received volume doesn't contain enough material to reach the workflow's target input ng, even using the entire tube. actual_Input_ng records what was actually achievable instead.", verbose_name='Insufficient Material'),
        ),
        migrations.AddField(
            model_name='workflowtype',
            name='logs_plate_and_well',
            field=models.BooleanField(default=True, help_text='True (TotalRNA, DNA PCR-Free): lab logs an index Plate Set (A-D) + Well, and the LIMS looks up the UDI + sequences. False (KAPA, Small RNA): only one plate set exists for this kit, so the lab logs the UDI number directly.', verbose_name='Logs Plate Set + Well'),
        ),
        migrations.AddField(
            model_name='workflowtype',
            name='qia_spike_volume_ul',
            field=models.FloatField(blank=True, default=None, help_text='Fixed volume added per well when uses_qia_spike is True (e.g. 0.5 µL).', null=True, verbose_name='QIA Spike Volume (µL)'),
        ),
        migrations.AddField(
            model_name='workflowtype',
            name='uses_qia_spike',
            field=models.BooleanField(default=False, help_text='Small RNA prep only: adds a fixed volume of QIAseq Spike-In (internal control) to every well, on top of the RNA + diluent volume. When True, target_volume_ul should be the RNA+diluent fill volume ONLY (e.g. 4.5 µL), not including the spike.', verbose_name='Uses QIA Spike'),
        ),
    ]
